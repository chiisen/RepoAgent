# 遺留專案分層重構指引（通用規範）

> 可直接複製到任何專案。目的：讓 AI Agent（或人類）在**不破壞既有行為**的前提下，把扁平單層的程式碼收斂成分層架構。
> 本文件所有 `<...>` 皆為佔位，請依專案替換（例如 `<test>`、`<typecheck>`、`<組裝根路徑>`）。
> 實例出自 RepoAgent 後端重構（PR #18，80 檔、+4,382/−1,871、93→100 測試），紀錄見該專案的 `docs/refactor/`。

## 0. 核心原則（違反其一即停止）

1. **先搬家，後改行為。** 結構變動與行為變動必須是可分離的步驟。混在一起，任何紅色都無法歸因。
2. **契約神聖。** 對外介面（HTTP API、WS 事件、DB schema、CLI 參數與輸出、檔案格式）在重構期間逐位元不變。要改契約是**另一個** PR。
3. **每一步都全綠。** 沒有「先紅著，最後一起修」。——重構是連續小幅前進，不是一次跳躍。
4. **測試不動就是證據。** 目標是讓既有測試**不需修改內容**即維持綠燈；這是「沒有改壞」最強的訊號。若必須改測試，先問：是斷言在描述實作細節，還是我真的改了行為？
5. **不順手。** 不改功能、不換套件、不重寫邏輯、不重排 UI。看到想改的東西 → 開 issue，不要現在做。

---

## 1. 開工前：鎖定不可變契約

動手前先產出「契約清單」，逐條寫明**如何被驗證**。沒有對應測試的契約，先補測試（步驟 2）。

| 契約類別 | 具體內容 | 驗證方式 |
| --- | --- | --- |
| HTTP API | 路徑、method、request／response 欄位名、狀態碼、錯誤碼字串 | `<test>` 或手動 `curl` 對照 |
| 事件／訊息 | 事件名稱、payload 欄位、觸發時機 | 事件斷言測試 |
| DB | 表名、欄位名、型別、`user_version`、migration 行為 | 開既有 DB 檔驗證 |
| CLI／程序 | 參數、stdout 格式、exit code | 既有 e2e 或腳本 |
| 檔案格式 | 設定檔欄位、log 格式 | 讀寫往返測試 |
| 使用者可見行為 | 「按下 X 之後畫面上會出現 Y」、「設定改完重整要還在」 | **最容易漏，見 §7.1** |

> 若契約無測試覆蓋 → 這條契約在重構中就是裸的。先補測試，或明確記錄為「已知無防護」的風險並告知使用者。

---

## 2. 建立安全網

1. 先跑一次完整驗證，記錄**基準數字**：測試數／檔案數、`<typecheck>` 錯誤數、`<lint>` 結果。
   - 例：`93 tests / 17 files`、`tsc --noEmit` 0 error。
2. 之後每一步重跑，任何偏離基準的紅色都當**迴歸**處理，不當「等一下再修」。
3. 找出並標記**跳過／停用**的測試（`test.skip`、`xit`、`@Ignore`、`--exclude`）。
   - **這是最高風險區**：被跳過的測試正是重構前就已失效的路徑，重構後會無聲延續。逐一確認 skip 的原因是否仍然成立。
   - 例：某 e2e `test.skip` 是因為「沒 build 前端就沒有該 DOM」→ 這不是環境問題，而是**功能在該情境下真的壞了**。
4. 若某分支完全無測試，先補一個「釘住現況」的測試（characterization test），**斷言現有行為**（即使你覺得它是 bug），再開始重構；把 bug 開成 issue 另外處理。

---

## 3. 依賴方向盤點

畫出「誰 import 誰」，特別是：

- 模組層級的 `new`／單例／可變狀態（`export const db = ...`、`let cache`）。
- 直接呼叫外部世界的點：資料庫、`spawn`／`exec`、`fs`、HTTP client、時鐘、亂數、環境變數。
- 循環依賴與雙向 import。
- **重複實作**：同一段工具函式存在多份（用「函式名重複」或相似片段搜尋）。

產出清單：`模組 → 它現在直接依賴什麼（應被抽象成介面）`。

---

## 4. 目標結構設計

分層命名可依專案調整，但**依賴方向必須單向由外而內**：

```
邊界層  <routes|controllers|handlers|cli|ui>
   ↓ 依賴
應用層  <application|services|usecases>      ← 業務流程，只認介面
   ↓ 依賴
核心層  <domain|core|model>                   ← 型別 + 介面 + 純函式，零外部依賴
   ↑ 實作
基礎層  <infrastructure|adapters|gateways>    ← DB / 網路 / 檔案 / 程序
組裝根  <composition|container|bootstrap>     ← 唯一 new 具體實例之處
```

設計介面的方法：**由使用端反推**，而不是「每個 class 包一層介面」。

- 先看 service 實際需要什麼能力（「我需要拿到 repo 清單」），再定義最小介面（`IRepoRepository`）。
- 介面放在核心層，實作放在基礎層。
- **不要為了對稱而抽介面**：若某個東西永遠只有一個實作且不碰外部世界（純計算），就不需要介面。

驗收檢查：核心層**不得** import 任何其他層；基礎層可 import 核心層；應用層可 import 核心層；只有組裝根可以同時認識所有層。

---

## 5. 遷移手法

### 5.1 順序

1. 抽核心層（純移動型別／常數／純函式，不改邏輯）。
2. 定義介面（由使用端反推）。
3. 抽基礎層（把外部世界呼叫搬進去並 `implements` 介面）——**此步不改行為，只是搬家**。
4. 抽應用層（改為建構子注入，移除模組層級 `new` 與可變狀態）。
5. 建組裝根（集中所有實例建立）。
6. 邊界層改為只依賴應用層。
7. 加相容層（見 5.2）。
8. 自我 Review → 修正 → 收斂死碼。

### 5.2 相容層（shim / re-export）的用法

當既有測試或呼叫端大量 import 舊路徑時，**不要同時改結構又改 import**。做法：

- 舊路徑檔案改成 `export { X } from '<新路徑>'` 並標 `@deprecated`。
- 效果：既有測試**一字不改**即全綠 → 直接證明「行為未變」。
- 這層是**技術債，不是設計**。必須同時開 issue 記錄拆除計畫，並在文件與 CHANGELOG 標明。
- 拆除方式：分批把呼叫端改到新路徑，每批跑一次測試，全數改完後刪除整層。

### 5.3 分批切分的原則

- 一個 commit 只做一類事（搬家 / 注入 / 清死碼 / 修 bug）。**bug 修復務必獨立 commit**，方便回溯與 revert。
- 每個 commit 都要能獨立通過驗證。

---

## 6. 依賴注入與組裝根

- 協作者一律**建構子注入**，不在方法內 `new`。
- **唯一例外**是組裝根：所有具體實例的建立集中一處，可一眼檢視整個服務的外部依賴面。
- 禁止用「參數鴨子型別探測」區分呼叫端（例如 `if ('db' in arg)`）。若一個入口需要兩種輸入，就**拆成兩個明確入口**（例：`createApp(container)` 與 `createAppWithDb(db)`），否則對呼叫端是隱性契約。
- 模組層級可變狀態（單例、快取）要嘛移進組裝根，要嘛明確標示生命週期。

---

## 7. 自我 Review 檢查表

重構的 Review 不能只讀 diff，要**對照行為**。以下依「重構最常引入的迴歸」排序。

### 7.1 CRITICAL：必查

| # | 失效模式 | 為什麼危險 | 怎麼查 |
| --- | --- | --- | --- |
| 1 | **條件式副作用**：`if (callback) doSideEffect()` | 把「必要行為」降級成「可選行為」。呼叫端沒傳參數 → 事件／通知／寫入靜默消失，但 API 仍回 200 | 搜尋所有「由參數決定要不要做某件副作用」的地方；問「這個副作用是業務必要，還是真的可選？」 |
| 2 | **setter 只驗證不賦值**：`setX(v) { return validate(v) }` | 看起來成功、回應 200，但狀態沒改；重整即還原 | 檢查每個 `set*`／`update*` 是否真的寫入 `this.*`；與同 class 其他 setter 比對語意 |
| 3 | **snapshot／getter 回傳內部參考** | 呼叫端改動會滲透進內部狀態 | 回傳處是否為 `{ ...this.x }` / `[...arr]`；巢狀結構是否仍共享（若共享需明示） |
| 4 | **介面留死方法** | 死方法會殘留錯誤實作。若它丟的是裸 `Error` 而非具名錯誤，一旦被使用會讓上層的錯誤碼判斷失效 | 逐個介面方法搜尋「有沒有呼叫端」；沒有就刪（連同介面聲明） |
| 5 | **enum／值域新增後消費端未同步** | 新值只在一處處理，其他 switch／映射漏掉 → 靜默落到 default | 新增任何常數／狀態值時，列出**所有**消費端逐一確認 |
| 6 | **併發與競態**：把同步流程改成非同步、抽出共用 registry | 原本靠單執行緒順序保證的不變式被打破 | 檢查 job／lock／queue 的註冊與移除時機；逾時與失敗路徑是否都會清理 |
| 7 | **信任邊界**：外部輸入（含 LLM 輸出）驗證被搬移後遺失 | 驗證留舊層、被搬走 → 新路徑無驗證 | 追蹤每個外部輸入從入口到使用點的完整驗證鏈 |
| 8 | **資料安全**：SQL／路徑拼接重構 | 參數化被拆成字串拼接、`path.resolve` 被拿掉 | 檢查任何 SQL 字串與 `spawn`／路徑參數 |

### 7.2 INFORMATIONAL

- 魔數與字串耦合（魔術字串應集中為常數，例：事件名）
- 重複實作（同一函式多份 → 應收斂為單一實作）
- 死碼：未被引用的 export、no-op 方法、自我引用的 shim、只為舊 API 存在的 static
- 測試缺口：新增的關鍵路徑有沒有斷言？**特別是「使用者可見行為」**
- 依賴方向瑕疵：核心層意外 import 了外層

### 7.3 反模式速查（看到就查）

```
if (onSomething) doSideEffect()        ← 條件式副作用
setX(v) { return validate(v) }         ← 只驗證不賦值
getSnapshot() { return this.state }    ← 回傳內部參考
if ('db' in arg) { ... }               ← 鴨子型別探測
static helper() 只被測試使用            ← 死方法
syncFromRegistry() { /* 自我引用 */ }   ← no-op 死碼
export type { X } // 沒人 import       ← 死 export
```

---

## 8. 驗收標準

全部滿足才算完成：

- [ ] 既有測試**未修改斷言**即全綠（僅允許配合 lint／format 調整 import 與寫法）。
- [ ] 測試數有增加（新增的測試要涵蓋 §7.1 找到的漏洞，特別是事件與持久化）。
- [ ] `<typecheck>` 0 error。
- [ ] `<lint>` 0 error。
- [ ] `<e2e>` 通過；**跳過的測試數目要與重構前一致**（新增 skip 必須有理由並開 issue）。
- [ ] `git diff --check` 乾淨。
- [ ] 對外契約清單（§1）逐條對照完成。
- [ ] `CHANGELOG.md` 已更新（依專案慣例）。
- [ ] 死碼與重複實作已處理，或明確開 issue 追蹤。
- [ ] 相容層（若保留）已標 `@deprecated` 並有拆除 issue。

**充要條件只有一句**：使用者看得見的行為，與重構前一致，且每一項都有斷言釘住。

---

## 9. 收尾

1. 更新 `CHANGELOG.md`（`[Unreleased]`）；若有版本號策略，由人工決定是否 cut 版本。
2. 開 issue 記錄已知取捨（相容層拆除、刻意縮減的檢查範圍、共享參考等），**不要留在 PR 描述裡就算了**。
3. 寫一份重構紀錄（放 `docs/refactor/<日期>-<主題>.md`），內容至少含：動機、目標架構、關鍵決定與理由、**過程中抓到的 bug 與根因**、驗證方式與數字、已知取捨與衍生 issue。
4. 回報時誠實區分：已驗證 / 未驗證 / 已知壞掉 / 刻意不做。

---

## 10. 導入其他專案

1. 複製本檔到目標專案根目錄（例：`REFACTOR-GUIDE.md`）。
2. 在該專案的 Agent 規範檔（`AGENTS.md`／`CLAUDE.md`／`GEMINI.md` 等）加一行：
   `> 重構一律依 [REFACTOR-GUIDE.md](./REFACTOR-GUIDE.md)。`
3. 依專案替換 §1 契約清單、§8 驗收指令的佔位（`<test>`／`<typecheck>`／`<lint>`／`<e2e>`）。
4. 與既有的 `PR-WORKFLOW.md`（分支／PR／Review／回覆）搭配使用：本檔管**怎麼重構**，那份管**怎麼送審**。
